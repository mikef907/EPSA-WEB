query Events {
  events {
    id
    parentId
    name
    description
    start
    end
    allDay
  }
}

query EventById($id: Float!) {
  event(id: $id) {
    id
    parentId
    name
    description
    start
    end
    allDay
  }
}
